# Bradax Broker / SDK - Exemplo de .env (Multi-Auth v1)
# Renomeie para .env e ajuste valores reais. Não commite segredos reais.

# === OBRIGATÓRIOS ===
BRADAX_JWT_SECRET=altere_esta_chave_master_segura_32+chars
OPENAI_API_KEY=sk-coloque_sua_chave_openai_aqui
BRADAX_ENV=development  # development|testing|staging|production
BRADAX_HUB_HOST=0.0.0.0
BRADAX_HUB_PORT=8000

# Expiração JWT (minutos)
BRADAX_JWT_EXPIRE_MINUTES=15

# === RATE LIMIT (opcional) ===
BRADAX_RATE_LIMIT_RPM=60
BRADAX_RATE_LIMIT_RPH=1000
BRADAX_MAX_CONCURRENT=10

# Timeout LLM (segundos)
BRADAX_HUB_LLM_TIMEOUT=180

# === SDK / Projeto ===
BRADAX_BROKER_URL=http://localhost:8000
BRADAX_PROJECT_ID=proj_real_001
# Preencha após gerar
BRADAX_PROJECT_TOKEN=

# Observações:
# 1. Apenas BRADAX_JWT_SECRET + OPENAI_API_KEY bastam para subir o broker.
# 2. Token por projeto é derivado (HMAC-SHA256 master + "bradax-jwt-v1::" + project_id)
# 3. Recrie tokens ao trocar o master secret.
# 4. Não há campo extra de secret em projects.json; somente project_id.
# 5. Variável unificada de ambiente: usar apenas BRADAX_ENV (BRADAX_ENVIRONMENT obsoleta).
